<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@4.6.2/dist/css/bootstrap.min.css">
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@4.6.2/dist/js/bootstrap.min.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@4.6.2/dist/js/bootstrap.bundle.min.js"></script>
    <title>Document</title>
</head>
<body>
    <div class="container">
        <div class="row">
            <div class="col-xl-5">
                <img style="width: 100%; height:80%" src="{{product.img.[0]}}" alt="">
                <div style="padding-top:2%; display:flex; justify-content:space-between" class="image">
                    {{#each product.img}}
                        <img style="width:10%; height: 18%" src="{{this}}" alt="">
                    {{/each}}
                </div>
            </div>
            <div style="padding: 60px" class="col-xl-7">
                <p class="name">Name: {{product.name}}</p>
                <p class="price">Price: {{product.price}}</p>
                <div style="padding-top:100px" class="button">

                <button>Buy Product</button>
                <button>
                    Show history
                </button>
                </div>
            </div>
        </div>
    </div>
    <div class="container">
        <div class="row">
            <div class="col-xl-5">
                <p>
                    Mô tả chi tiết: {{product.desc}}
                </p>
            </div>
            <div class="col-xl-7">
                <table border="1" style="border-collapse: collapse; width:100%">
                        <tr>
                            <td>Tên sản phẩm</td>
                            <td>Giá sản phẩm</td>
                        </tr>
                        <tr>
                            <td>{{product.vars.product1.name}}</td>
                            <td>{{product.vars.product1.price}}</td>
                        </tr>
                        <tr>
                            <td>{{product.vars.product2.name}}</td>
                            <td>{{product.vars.product2.price}}</td>
                        </tr>
                </table>
            </div>
        </div>
    </div>
</body>
</html>